# TL 三书技术分析日报：固定运行规范

版本：1.3
固定网址：https://tl-bond-analysis.netlify.app/  
执行时间：每个周一至周五 15:30（Asia/Shanghai）

本文件是日报自动化的唯一操作规范。调度提示必须先读取本文件，再执行任务。若调度提示与本文件冲突，以本文件为准；用户后续明确修改要求时，同步修改本文件并提交版本记录。

## 1. 任务目标

Codex 每个实际交易日收盘后亲自取得数据、完成分析、生成网页并发布。网页只负责展示，不得把旧报告改日期后重复发布。分析对象是中金所 30 年期国债期货 TL 的当日成交量主力月份合约，不能固定为 TL2612。

成功交付同时满足：数据真实且截止到完整收盘；充分调用“三书技术分析”知识库；结论明确且有反方证据；点位精确；形态有图；网页无交互故障和异常留白；GitHub 与 Netlify 发布成功。

## 2. 每次执行的硬性顺序

1. 判断当天是否为中金所实际交易日，并确认行情已经形成完整收盘。若不是交易日、数据尚未收齐或最新交易日没有变化，保持现有网页，不提交、不推送。
2. 枚举当日 TL 各月份合约，按日成交量识别主力；同时保存成交量、占比、次主力比例和前一交易日主力。存在换月歧义时明确披露。
3. 获取具体合约的日线、完成周线、当日日内行情、成交量和持仓量。实际合约用于执行点位，连续序列只用于长期背景；换月跳空不得解释成真实收益。
4. 获取当前主力具体合约最近 6 个可得交易日的合格 Tick，计算逐价成交量分布和多日八类交易结构趋势；主力换月时分段展示，禁止合并不同价格基准。
5. 校验数据后，读取 `technical-analysis-wiki` 的 `SKILL.md`、知识库 `HOME.md`、`分析入口.md`、数据要求、证据模型、公式状态及输出契约，再按当天结构深读相关方法页。不得调用已卸载的 `tl-technical-analysis`。
6. 先完成分析和结论，再生成 HTML。使用 `pingan-color-skill` 的主题文件与配色规范。
7. 在本地验证 HTML、图表、交互和日期；通过后才写入 `index.html`、提交并推送 `origin/main`。
8. 等待 Netlify 部署，验证固定网址 HTTP 200、页面标题、交易日期、主力合约和收盘价。任一关键环节失败时保留上一交易日报告。

## 3. 数据来源与质量

优先使用 `cjpy-data`，再按可用性用 JYDB、Wind 和 AKShare 交叉核验。数据源服务端目录必须先发现，不能凭记忆猜字段。每个来源记录：供应商、接口、代码、请求时间、实际首末时间、周期、行数、是否截断、缺失字段和失败原因。

至少检查：时间排序、重复记录、OHLC 约束、成交量与持仓量单位、交易时段、最后柱是否完成、日线与日内收盘是否一致。不得用缓存、插值或推测值冒充当日数据。关键来源不一致时先查代码、合约、时区、单位、复权/换月和字段定义；无法解释时降低置信度或停止发布。

## 4. 八类交易结构的不可降级门槛

多开、空开、多平、空平、多换、空换、双开、双平只允许来自逐笔数据直接提供的 `trade_type`、开平性质字段或可验证的真实逐笔开平标志。禁止根据分钟 K 线、价格涨跌、分钟成交量与持仓变化推断，禁止把混合成交的时间桶归为单一类型。

合格来源优先为东方财富掘金期货 Tick：`trade_type` 1—8，权重使用 `last_volume`。Token 只能从本机密钥文件或安全配置读取，绝不写入仓库、HTML、日志或回复。

本机已安装 `gm` SDK。东方财富掘金终端运行并登录时，使用仓库脚本读取终端的短期本机会话凭据并汇总，不保存账号、密码或 Token：

```powershell
C:\Users\15704\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts/eastmoney_tick_structure.py --symbol CFFEX.TL主力月份 --date YYYY-MM-DD --output work/eastmoney-tick-YYYY-MM-DD.json
```

脚本输出仍需与交易所日统计交叉核验；`publish_eight_types=true` 只表示 Tick 内部成交量闭合，不代替外部核验。终端未运行、登录失效或接口不可用时不得改用分钟估算。

发布八类占比前必须完成：

- 按 `last_volume` 计算各类型成交量；分母为当日全部 Tick 成交量。
- 单列非法、空值或无法识别的成交量及占比；不得强制八类凑满 100%。
- 核对 Tick 时间覆盖和成交量覆盖率。
- 核对最后 `cum_volume`、`cum_position` 与交易所日成交量、日终持仓量及持仓变化。
- 发现重复 Tick、时段缺口、累计量重置或闭合失败时，停止展示八类百分比。

没有合格逐笔数据时，页面写明“八类交易结构数据不可得”，不显示估算百分比。可以展示经日统计核验的价格—成交量—持仓量四象限，但必须说明它不能拆分八类成交。

### 4.1 核心密集成交区

后台综合20交易日、5交易日和关键波段锚定窗口，定位核心密集成交区域。同一具体合约，不混合不同合约价格。优先实际Tick，历史不足可使用明确标注的分钟典型价代理；不得将代理说成精确成交价分布。
前台只在精简点位地图及K线色带标注一个核心区域和数据口径，不展示多个POC数字、POC卡片或多窗口分布卡片。内部计算结果留档以供复核。不能称为未平仓筹码、挂单或必然反转点。

### 4.2 八类状态逐日变化

最近至少6个可得交易日，逐一列示多开、空开、多平、空平、多换、空换、双开、双平的每日占比，计算较昨日变化（百分点），在总表列示各状态，围绕关键变化综合分析。保留开仓型（多开＋空开＋双开）与平仓型（多平＋空平＋双平）的多日共轴曲线，辅助解释参与方式；八类逐项分析保持为核心。
分析各状态持续性、拐点及相互制约，结合价格和持仓说明支持证据与反方证据。区分占比和绝对手数，不能把占比上升自动等同新增资金或真实手数增加。

## 5. 三书技术分析的研究深度

价格结构优先，结合当天实际结构选取方法；不得加载全部 108 个方法制造篇幅，也不得按指标数量投票。完整日报至少落实：

- 日线、完成周线和日内三个层级的方向、峰谷、趋势成熟度与关键位置。
- 道氏趋势逻辑、趋势线或通道、支撑阻力及角色转换。
- 实际出现的反转/持续形态、缺口、突破与假突破过滤；没有合格命名形态时明确写“无已确认命名形态”。
- 成交量、OBV 和期货持仓量；区分价格、成交量和持仓量各自提供的信息。
- SMA/EMA、ROC、Wilder RSI、Wilder ATR、MACD及柱图，使用技能脚本约定并披露参数。只在数据与市场条件适用时加入相对强弱、市场宽度、周期、季节性、波浪或点数图。

均线、ROC、RSI、MACD等同源价格指标不能作为多份独立确认。未完成周线、需要未来数据才能确认的峰谷、单一指标交叉和候选形态不能写成已确认事实。

## 6. 结论与精确点位

首页必须首先回答：一句话总判断、趋势阶段与成熟度、核心矛盾、当前策略含义和置信度。随后给出支持证据、反方证据、主要情景、替代情景与下一交易日观察顺序。

严格区分观察事实、技术解释、候选信号、确认信号、执行条件和失效风险。向上与向下路径分别给明确的收盘确认价、盘中观察价（如适用）、失效价和下一目标。点位来自可复核的峰谷、趋势线、区间边界、缺口或波动口径；不得用过宽区间掩盖不确定性。退出原仓与建立反向新仓分别判断。

## 7. 网页与形态可视化

网页为中文单文件 HTML，采用白色主背景、`#FF6600` 主强调色和 `#002960` 深海军蓝结构色。首页总结优先，详细证据随后展开。

形态区固定展示“形态状态、方向结论、行动状态”。真实 K 线主图必须标出已确认峰谷、候选转折、边界线、突破确认价和失效价，并配随当日数据生成的简化结构图和图例。方向未确认时醒目显示“尚未确认·中性等待”，上下路径等权；确认后高亮已触发方向，并把另一方向降为失效或替代情景。

“形态标注”按钮必须能实际显示和隐藏图层。主图窗口覆盖所有被引用的峰谷日期；否则扩大窗口或取消标注。图表卡片高度由内容决定，不得被相邻卡片拉伸，主图下方不得出现大块空白。页面同时保留多周期状态、指标解释边界、推理链、量价持仓、证据账本、反方检查、数据质量和方法来源。

## 8. 发布前验收

- 最新交易日、主力合约、收盘价与源数据一致。
- 一句话判断与详细证据没有方向矛盾。
- 所有确认价和失效价在正文、图表与情景卡中一致。
- 点位地图只标注一个核心密集区及少数必要支撑阻力，K线图使用色带；不得重复列示POC。
- 多日交易结构至少覆盖最近 6 个可得交易日，趋势图、逐日表和文字结论一致；每日成交量与八类识别均闭合。
- 命名形态满足前置趋势、边界、确认和失败条件；候选没有写成确认。
- 八类交易结构满足第 4 节门槛，否则完全不显示估算百分比。
- 实际点击成交量、关键线和形态标注按钮，确认状态与图层变化且可恢复。
- 页面无横向溢出、遮挡、异常大块留白或空链接。
- 推送后固定网址返回 HTTP 200，标题和交易日期正确。

## 9. 成功与失败通知

成功后报告：一句话判断、实际主力合约、收盘价、四个最重要的确认/失效点、八类结构的数据状态、数据质量摘要和固定网址。

非交易日或没有新收盘数据时保持安静。数据、分析校验、GitHub 推送或 Netlify 发布失败时不覆盖上一版，只通知具体失败环节、已保留的最后有效交易日和下一步所需条件。

## 10. GitHub成品与部署额度

先在本地完成计算、HTML生成与验证，再将最终index.html同步复制到dist/index.html。GitHub保存已完成的静态成品；netlify.toml只发布dist目录，不在Netlify执行分析、生成页面或调用AI。
常规日报只推送一次正式成品。中间修改留在本地；确需推送中间记录时使用[skip netlify]提交标记。禁止通过反复正式部署来预览。用户明确要求当天修改时可增加一次最终发布。
Netlify信用额度套餐按成功正式部署消耗额度，GitHub制作不消除此费用；不得承诺免额度。发布前检查静态成品、JS语法、数据和文案一致性，发布后核对HTTP200及内容标记。

## 11. 交易结构重点与研究深度

页面先给核心结论，再按主动权、最大变化、反方证据、阶段定位四项展开。核对占比及绝对手数，区别优势水平与扩大速度。可以分解主动买卖份额差，但不能冒充价格因果归因。开平标签差不能等同多头或空头真实净持仓。关于尾盘由何种交易推动、颈线上方持续时间等论断须逐时匹配数据，未核验不得编写。保留汇总曲线与八类逐日表，不再单独绘制八类状态小图，不重复列示总表已有信息；最后写清下一交易日升级、降级条件。

展示去重：交易结构固定为核心结论、重点分析、开仓型/平仓型多日曲线和一个八类状态总表；删除八类独立小图和逐类重复卡片。分析引用必要数字以支持结论，不机械复述整张表。

点位说明必须交代来源（具体日期、峰谷或计算窗口）、为何关注、触碰与突破的区别，以及确认周期和阈值。明确观察价不等于确认价，不使用“即时续弱确认”等含混缩写。保留少数点位，不因解释增加点位数量。低点仅是事实，突破信号必须写明完整K线条件。

## 12. 最新展示要求：适度精简（优先于前述展示形式）

保留主图、形态、多周期、量价持仓、情景判断、反方证据和八类六日总表。交易结构不再画任何曲线，删除01—04长卡片和逐状态小图；采用一句核心结论、三条重点（主动权、关键变化及反方、六日开仓型/平仓型阶段变化），用文字交代开平汇总及包含双开双平口径。深度体现在取证和重点判断，而非长篇幅。点位保留简短来源与必要确认周期、阈值；不增加点位。合并重复推理，证据账本可折叠，不删减关键事实和风险边界。

最新点位展示要求：点位地图每行只突出点位与意义标签（如结构转强关口、核心成交阻力区、短线修复关口、短线支撑、日线关键支撑），不展开来源、推导和确认条件，不再添加解释框。分钟估算标签保留以识别精度；确认周期与阈值仍在情景分析中保留。此要求优先于之前的点位详细解释规则。

点位覆盖最新要求：不固定每侧三个，保留实际重要的主要前高、次级高点、结构转强位、核心密集区、弱势解除位、修复位及各级关键低点。相近同源点位合并，不为凑数添加指标点。前台仍仅价格＋意义标签，不展开长解释；主图可筛选重要线避免标签挤压，点位地图完整列出。

## 13. 点位完整性检查（最新优先要求）

不得只保留一个全局核心密集区。后台同时检查20日、5日和波段锚定分布，分别覆盖现价附近、上方、下方；保留有独立意义的局部密集区及主要峰谷，合并同区域观察位。前台仍仅价格区域、意义标签和简短精度标识，不恢复POC卡片或长解释。支撑有效性不能仅凭历史密集成交确认。
每次对各窗口候选带逐项记录show/merge/omit及原因，用scripts/audit_profile_coverage.py校验决策覆盖；候选来源亦须审查局部峰和低成交通道，不能将Top-N当作全市场完整性的保证。历史Tick不足时披露分钟代理，不混用窗口占比分母。发布前核对现价局部区、上方阻力区、下方承接区有结果或明确数据缺口，再检查与峰谷和情景一致性。该检查减少遗漏，不保证行情分析绝无遗漏。

## 14. 整页完整性审核（每次发布必做）

不仅审核点位。逐项核对：实际主力与换月、交易日和完整收盘、各周期OHLCV、波段峰谷和时间可知性、形态有效性及失败修复、趋势线触点与越线、真缺口与低开、均线和动量、ATR波动尺度、量价与OBV、持仓与八类六日结构、多窗口局部成交区、各情景确认与失效、反方证据及数据限制。逐项记录已覆盖／不适用及理由／待核验，不强凑形态或指标。重要结论须有数据和三书方法依据，研究完整不等于每项都增加前台卡片。

用scripts/audit_report_data.py复核当日各周期、六日Tick和分钟汇总（脚本目前是2026-09-11审计快照，后续须改用当次交易日输入，禁止直接复用旧结果）。各窗口同口径；周线样本不足不输出未初始化MACD；供应商竞价记录和末尾短柱不得当完整小时确认。持仓若只有单源，不能称跨源闭合。收盘后的零量记录不算成交，核验Tick重复、时间戳及累计量。区分占比上升与绝对量上升，区分状态标签与真实净持仓，不作未经逐时验证的价格因果归因。

检查首页摘要、图形、点位和情景一致；下行情景不得遗漏有意义的下方成交区。核查图形开关、情景切换、锚点、移动端和空白；index.html与dist一致。所有检查完成后一次发布，在线核验日期、主力、价格和内容版本。审计记录保存数据结果与未解决限制；不宣称绝无遗漏。

发布环境现状（2026-09-11）：Netlify本期生产部署额度耗尽，跳过05e7e85；固定网址停留fdd09b4。GitHub Pages已启用，可作为明确标注的备用阅读地址https://lulichen724-creator.github.io/tl2612-analysis-20260910/ 。每日仍完成分析及GitHub成品；分别核验两个站点，不能将GitHub成功写成Netlify成功。不要反复触发受限部署、不要自行升级付费；恢复后再核验固定网址最新版本。

## 15. 多尺度、多种形态与边界稳健性（2026-09-11最新要求）

不能只看最近15—30日、也不能只讲三角形。背景尽量覆盖TL主力连续上市以来历史，至少拉长到能识别上一轮主要趋势；具体主力合约使用全部可得日线及完成周线，结合近1—3月和必要日内结构。连续序列须披露换月及未复权限制，不将跨合约历史点位直接用作当月执行价格。检查返回行数和截断状态，不能用接口元数据的latest_time冒充已返回完整数据。

深读三书相关方法与章节，检查峰谷、区间、三角、旗形/三角旗、通道/楔形、多重顶底、头肩、圆形，以及艾略特推动/ABC、比例回撤；按有证据的候选展示，不要求每日所有形态成立。前台多视角切换，保留图、短结论、成立/缺失条件和否定条件。证据不足须给具体理由，不能以“无需强凑”为由跳过用户要求的长周期和波浪检查。

三角至少核锚点有效性与可知日期，使用实际交易日横轴；并列极值、统一收盘和旧线敏感性，不能取当天尚未反弹的低点构造陡线后宣布当日形态坏掉。两次触点不等于充分验证，重画保留旧记录；轻穿、幅度过滤、连续收盘、水平波谷破坏分层。不得为保留某个形态任意移动线，也不得将新选ATR容差反填为过去已实施的规则。0.25ATR仅本次诊断样例，未来如沿用须事前注明，不能称三书共识或已回测。

波浪最多保留有根据的主计数和一至两套替代，注明浪级、峰谷、内部子浪核验程度、价格失效条件。未确认5浪启动不得画成已启动；4浪候选底跌破与整套计数失败分开。期货日内重叠例外不得随意移到日线；比例目标仅随结构条件触发，不是必达价。每日比较昨天计数与今天新增证据，保留修订原因。

本次纠正：撤回仅凭9/10未确认低点116.14画线而判“三角候选失效”的强结论；短线弱势事实保留，形态改为候选边界受压、尚未稳健确认下破。未来不固化该日期和方向。scripts/build_pattern_study_20260911.py是本日研究快照，后续不得直接重放替代每日重新分析。

## 16. 最新形态展示偏好（优先于第15节的前台要求）

用户要求边界采用有实际转折依据的较宽松统一口径，不再前台展示边界敏感性、多线比较或旧线。后台保留选点和修订记录。当前使用8/28—9/2极值下沿与0.25ATR观察带；后续每日报告需按实际合约和有效锚点更新，不直接照抄历史值，不为了包住当天收盘随意挪线。规则是整理者的宽松观察方案，并非三书统一阈值或回测结论。

背景前台缩到近四个月/当前主要中期波段，不再以三年背景作默认面板；必要更长历史仅后台核验。四视角为当前收敛形态、近四个月背景、五浪与三浪、其他形态对照。

波浪必须先给清晰标准示意（明确非行情预测），再对照真实图。五浪推动1—5与ABC调整分开；锯齿5-3-5、平台3-3-5、三角五个三浪段不能混淆。大级别用(1)—(5)，小级别用a—b—c等标注；第四浪内部ABC不是天然互斥替代方案。实际未完成5浪或C浪时明确写未完成，不画未来价位为事实；只有真正互斥的计数才称替代。优先讲清结构标准、实际满足程度、缺失子浪和确认/撤销条件。

## 17. 最新紧凑K线展示（优先于第15—16节的前台形式）

技术形态板块压缩为一张可切换K线图，波浪标在真实K线上；保留波浪总览、调整细分、收敛整理三个视角，一次只显示一张图。删除大幅教学示意、长表和重复说明，每图仅短结论与确认/失效条件。总览覆盖近四个月，细分近期。图高约300像素，移动端允许横向查看。

不同浪级或有证据的不同计数组使用不同颜色，当前橙色为中级推动候选、深蓝为小级别abc，灰色K线为实际行情；颜色与括号/字母同时区分，不仅依赖颜色。不强行新增多套五浪以满足配色，不把尚未确认的第5浪画成实际走势。后台仍核查各类形态及标准，前台只呈现有效重点。

## 18. 数据看板布局（最新前台要求）

顶部集中结论、反方证据和四项KPI（收盘、成交量、持仓、主动卖买差），显示同口径较昨日变化和数据截至时间。主区域四视图：行情总览、形态与周期、交易结构、研究依据；一次显示一个，支持分享视图锚点。总览主图与完整重要点位并列，情景和反方简洁；量价多窗口证据按需展开。形态保留紧凑可切换K线与不同颜色浪级；交易结构保留八类六日总表、三条重点和CSV导出，无重复曲线。方法与源精度归研究依据，不能用简化布局省略后台研究。每日报告重新计算变化，禁止复用9月11日快照。保留平安配色、移动端布局和清晰状态标签；验证切换、锚点、图表、内容覆盖，明确区分程序检查与真实浏览器验收。

## 19. 全部展开阅读（2026-09-12最新要求，覆盖此前折叠展示要求）

用户要求直接阅读，无需点击才能看到分析。行情、形态周期、交易结构、研究依据全部连续展开；顶部导航仅跳转。形态三张紧凑K线、各情景、量价和指标说明、证据默认全部可见，不使用隐藏标签页或折叠详情。保留图表图层开关作为可选工具，默认显示；仍控制留白与重复文字，不用展开作为恢复冗长内容的理由。

## 20. 重点与紧凑排版（2026-09-13）

首页综合摘要必须覆盖多周期背景、现价关键位置、形态确认程度、动量、量价持仓、交易结构主变化与反方、下一步条件；三条分组短段突出重点，不堆指标。每个模块先给一句核心结论。波浪总览与abc调整合为一张真实K线，图内局部放大并区分颜色浪级；不重复单独细分图。所有内容展开：主图全宽，重要点位在图下多列紧凑列示，情景横向并列、反方另成行，手机纵排；避免长短卡片并排产生大片空洞。不删有意义点位，不把候选写成确认。
